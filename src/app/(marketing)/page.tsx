import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "TYIMS LMS — Learn Without Limits",
};

export default function HomePage() {
  return (
    <main>
      <h1>TYIMS LMS — Coming Soon</h1>
      {/* Marketing components will be built here */}
    </main>
  );
}
